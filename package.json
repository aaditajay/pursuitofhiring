{
  "name": "pursuit-of-hiring-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../build && cp -r build ../build"
  }
}
